{
  "name": "data-structures",
  "version": "0.0.1",
  "description": "All common Data Structures & Algorithms implement in TypeScript",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "https://github.com/kopenkinda/data-structures",
  "author": "Dmitrii Kopenkin @kopenkinda",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "clean": "rimraf lib/",
    "lint": "tslint -c tslint.json 'src/**/*.ts' --fix --force",
    "lint:tests": "tslint -c tslint.json 'test/**/*.ts' --fix --force"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "jest": "^26.4.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.2.0",
    "tslint": "^6.1.3",
    "typescript": "^3.9.7"
  }
}
